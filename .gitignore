# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json

# Multi-agent live state (kanban + locks) — config.json stays committed
active_tasks.md
active_files.md
