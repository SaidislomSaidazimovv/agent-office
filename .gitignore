# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json

# Multi-agent live state (kanban + locks) — config.json stays committed
active_tasks.md
active_files.md

# Texture source files (not used by the web app — only the PNG maps are)
*.blend
*.blend1
*.usdc
*.mtlx
*.tres

# Local self-verify helper (puppeteer, not a saved dep)
_shot.cjs
_shot_agents.cjs
scratchpad/

# TS build info
*.tsbuildinfo

# Packaged extension (regenerable, large)
*.vsix
